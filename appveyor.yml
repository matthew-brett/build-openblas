# vim ft=yaml
# CI on Windows via appveyor

environment:
    global:
      MSYS2_ROOT: c:\msys64
      BUILD_COMMIT: v0.2.19
      BUILD_ROOT: c:\opt

    matrix:
      - PYTHON: "C:\\Python27"
      - PYTHON: "C:\\Python27-x64"

build_script:
    - build_script.bat

artifacts:
    - path: openblas_*.*
